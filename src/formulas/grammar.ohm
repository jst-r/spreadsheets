Formula {
	Exp = AddExp
	
	AddExp = AddExp "+" MulExp  -- plus
    	|    AddExp "-" MulExp  -- minus
        |    MulExp

	MulExp = MulExp "*" number	-- multiply
		|	 MulExp "/" number	-- divide
		|	 number

	
	number = digit+
}